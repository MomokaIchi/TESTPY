# What happens here?
def test(x):
    if x > 5:
        return True
    print("Hello")
    return False

# If input(=x) is more than 5, output is True (bool)
# Is input(=x) is 5 or less, show the word "Hello" and output is False (bool)